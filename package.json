{
  "name": "ts-node-fetch-client",
  "version": "1.0.2",
  "description": "",
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "compile": "tsc",
    "test": "jest",
    "push": "npm run test && npm run compile && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tsk-murakami/node-fetch-client.git"
  },
  "author": "tasukumurakami",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/tsk-murakami/node-fetch-client/issues"
  },
  "homepage": "https://github.com/tsk-murakami/node-fetch-client#readme",
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "@types/json-server": "^0.14.3",
    "@types/node-fetch": "^2.5.8",
    "jest": "^26.6.3",
    "json-server": "^0.16.3",
    "ts-jest": "^26.5.1",
    "typescript": "^3.7.2"
  },
  "dependencies": {
    "node-fetch": "^2.6.1"
  },
  "jest": {
    "moduleFileExtensions": [
      "ts",
      "js"
    ],
    "transform": {
      "^.+\\.ts$": "ts-jest"
    },
    "globals": {
      "ts-jest": {
        "tsconfig": "tsconfig.json"
      }
    },
    "testMatch": [
      "**/tests/**/*.test.ts"
    ]
  }
}
